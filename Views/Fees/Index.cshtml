@model IEnumerable<GroupThreeTrailerParkProject.Models.Fee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AppliesTo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AppliesTo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FeeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FeeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FeeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
